[project]
name = "sf-backtester"
version = "0.2.0"
description = "Multi-node parallelized backtesting tool for BYU Silver Fund Quant Team."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "polars>=1.37.1",
    "pyyaml>=6.0.3",
    "sf-quant>=0.2.4",
]

[project.scripts]
sf_backtester = "sf_backtester.cli:main"
