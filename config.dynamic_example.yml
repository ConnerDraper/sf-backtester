# Example configuration for dynamic backtest with gamma calibration
# Copy this file and modify for your backtest
# Use: sf-backtester dynamic config.dynamic_example.yml

signal_name: momentum
data_path: /path/to/project/temp/alphas.parquet

# Dynamic optimization parameters
# initial_gamma is the starting point for gamma calibration
# target_active_risk is the desired annualized active risk (tracking error)
initial_gamma: 100.0
target_active_risk: 0.05

# Required: your project setup
project_root: /path/to/project
byu_email: your.email@byu.edu

# Optimization constraints
constraints:
  - ZeroBeta
  - ZeroInvestment

# SLURM configuration (all optional with defaults)
slurm:
  n_cpus: 8
  mem: 32G
  time: "06:00:00"
  mail_type: BEGIN,END,FAIL
  max_concurrent_jobs: 31

# Optional: override default output paths
# output_dir: /custom/path/to/weights
# logs_dir: /custom/path/to/logs
